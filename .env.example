# 复制为同目录下的 .env 并填写（已加入 .gitignore）
# 格式：KEY=value，勿加引号

AI_API_KEY=
AI_ENDPOINT=
AI_MODEL=
AI_EMBEDDING_MODEL=

# 启用综测细则 AI 问答
SPRING_PROFILES_ACTIVE=default,ai

# Chroma 配置
CHROMA_HOST=http://127.0.0.1
CHROMA_PORT=8000

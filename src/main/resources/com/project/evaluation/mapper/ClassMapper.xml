<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.evaluation.mapper.ClassMapper">

    <select id="paginationQuery" resultType="com.project.evaluation.entity.Class">
        SELECT * FROM `sys_class`
        <where>
            <if test="collegeIds != null and collegeIds.size() &gt; 0">
                AND college_id IN
                <foreach collection="collegeIds" item="cid" open="(" separator="," close=")">#{cid}</foreach>
            </if>
            <if test="gradeYears != null and gradeYears.size() &gt; 0">
                AND grade_year IN
                <foreach collection="gradeYears" item="gy" open="(" separator="," close=")">#{gy}</foreach>
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectByIds" resultType="com.project.evaluation.entity.Class">
        SELECT * FROM `sys_class`
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY id
    </select>
</mapper>